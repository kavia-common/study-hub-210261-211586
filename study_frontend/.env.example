# Optional. Defaults to "/api" if not set.
# REACT_APP_API_BASE_URL=/api

# Optional. If backend WebSocket endpoint is hosted elsewhere, expose a full URL.
# Example: wss://example.com/ws/chat/
# REACT_APP_WS_CHAT_URL=
